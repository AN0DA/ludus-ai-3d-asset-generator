# Environment Variables for AI 3D Asset Generator
# Copy this file to .env and fill in your actual values

# Environment Configuration
ENVIRONMENT=development

# =================================================================
# LLM CONFIGURATION
# =================================================================
# Note: Cost tracking has been removed from the LLM configuration.
# The system now focuses on generation parameters and output quality.

# LLM Configuration - Direct Settings (used by LLMGenerator)
LLM__API_KEY=sk-your-openai-api-key-here
LLM__BASE_URL=https://api.openai.com/v1
LLM__MODEL=gpt-4.1

# LLM Request Configuration
LLM__TIMEOUT=60                    # Request timeout in seconds
LLM__MAX_RETRIES=3                 # Maximum number of retry attempts
LLM__RATE_LIMIT_REQUESTS=30        # Requests per rate limit window
LLM__RATE_LIMIT_WINDOW=60          # Rate limit window in seconds

# LLM Generation Parameters
LLM__MAX_TOKENS=2000               # Maximum tokens in response
LLM__TEMPERATURE=0.7               # Creativity level (0.0-2.0)
LLM__TOP_P=0.9                     # Nucleus sampling threshold (0.0-1.0)
LLM__FREQUENCY_PENALTY=0.1         # Penalty for frequent tokens (-2.0-2.0)
LLM__PRESENCE_PENALTY=0.1          # Penalty for present tokens (-2.0-2.0)

# LLM Output Configuration
LLM__OUTPUT_FORMAT=json            # Output format: json, structured_text, markdown
LLM__INCLUDE_EXAMPLES=true         # Include few-shot examples in prompts
LLM__VALIDATE_JSON=true            # Validate JSON responses with Pydantic

# =================================================================
# STORAGE CONFIGURATION
# =================================================================

# S3-Compatible Storage Configuration
# Works with AWS S3, MinIO, CloudFlare R2, DigitalOcean Spaces, Wasabi, Backblaze B2, etc.
STORAGE_ACCESS_KEY_ID=your-access-key-id
STORAGE_SECRET_ACCESS_KEY=your-secret-access-key
STORAGE_REGION=us-east-1
STORAGE_BUCKET_NAME=your-bucket-name

# S3-Compatible Storage - Custom Endpoint (for non-AWS services)
# Examples:
# MinIO: http://localhost:9000 or https://minio.yourdomain.com
# CloudFlare R2: https://your-account-id.r2.cloudflarestorage.com
# DigitalOcean Spaces: https://nyc3.digitaloceanspaces.com
# Wasabi: https://s3.us-west-1.wasabisys.com
# STORAGE_ENDPOINT_URL=https://your-s3-compatible-endpoint.com

# S3-Compatible Storage - CDN Domain (optional)
# STORAGE_CDN_DOMAIN=assets.yourdomain.com

# S3-Compatible Storage - Advanced Settings
# STORAGE_USE_SSL=true
# STORAGE_MAX_FILE_SIZE=209715200
# STORAGE_MULTIPART_THRESHOLD=67108864

# 3D Generation Services
THREED_GENERATION__MESHY_API_KEY=your-meshy-api-key
THREED_GENERATION__KAEDIM_API_KEY=your-kaedim-api-key

# Database Configuration
DATABASE__URL=postgresql://user:password@localhost:5432/ai_3d_assets

# Security Configuration
SECURITY__SECRET_KEY=your-super-secret-key-at-least-32-characters-long

# Gradio Configuration
GRADIO__HOST=127.0.0.1
GRADIO__PORT=7860
GRADIO__SHARE=false

# Logging Configuration
LOGGING__LEVEL=DEBUG
LOGGING__FILE_PATH=./logs/app.log

# Application Settings
APP_NAME=AI 3D Asset Generator
APP_VERSION=0.1.0
TEMP_DIR=./temp/ai-3d-assets
MAX_CONCURRENT_GENERATIONS=3

# Optional: Override specific settings
# LLM__MAX_TOKENS=4096
# LLM__TEMPERATURE=0.8
# LLM__TOP_P=0.95
# LLM__FREQUENCY_PENALTY=0.0
# LLM__PRESENCE_PENALTY=0.0
# LLM__OUTPUT_FORMAT=structured_text
# LLM__INCLUDE_EXAMPLES=false
# LLM__VALIDATE_JSON=false
# THREED_GENERATION__QUALITY_PRESET=high
# GRADIO__THEME=soft
